// Jenkinsfile (Declarative Pipeline)
/* Requires the Docker Pipeline plugin */
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Your build steps
                echo "Building..."
            }
        }

        stage('Test') {
            steps {
                // Your testing steps
                echo "Testing..."
            }
        }

        stage('Deploy') {
            steps {
                // Your deployment steps
                echo "Deploying..."
            }
        }
    }
}
